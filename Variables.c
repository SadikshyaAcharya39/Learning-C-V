#include <stdio.h>

int main()
{

    // Printing values
    int num ;
    num = 15;

    // Not able to get the value we want
    // printf(num);    Nothing happens

    printf("%d", num);

    return 0;
}